#include "main.h"

/**
* _islower - fin lowercase character
* @c: character to be checked
* Return: 1 for lowercase character or  0 for others
*/

int _islower(int c)
{
	while (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
